using std::IO

fn main:
    set first: "Newton "
    set last: "Language"

    ; Basic Concat
    set full: $first << $last
    IO.println $full

    ; Chained Concat (Works because left-associative)
    IO.println "Welcome to " << $full << " v1.0"
end
